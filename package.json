{
  "name": "manrpg-pwa-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "worker:check": "tsc --noEmit -p tsconfig.worker.json"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.2.4",
    "vitest": "^4.1.7"
  }
}
